#ifndef VKCPP_RENDER_SWAPCHAIN_IMAGE_H
#define VKCPP_RENDER_SWAPCHAIN_IMAGE_H

#include "vulkan_header.h"

namespace vkcpp
{
    class Image
    {
    private:
        VkImage handle_;

    public:
    }; // class Image

} // namespace vkcpp

#endif // #ifndef VKCPP_RENDER_SWAPCHAIN_IMAGE_H